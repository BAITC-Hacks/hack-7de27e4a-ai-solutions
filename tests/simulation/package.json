{
  "name": "career-quest-employee-validation",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node prepare-standalone.mjs && vitest run --config vitest.config.mjs --configLoader native",
    "typecheck": "node prepare-standalone.mjs && tsc --noEmit -p .standalone/tsconfig.json",
    "build": "node prepare-standalone.mjs && next build .standalone --webpack",
    "dev": "node prepare-standalone.mjs && next dev .standalone --hostname 127.0.0.1 --port 3100"
  },
  "dependencies": {
    "next": "16.2.9",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "zustand": "5.0.14"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "5.9.3",
    "vitest": "^3.2.0"
  }
}
